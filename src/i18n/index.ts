import en from './en';
import de from './de';
import fr from './fr';
import es from './es';

// Export all translations
export const translations = {
  en,
  de,
  fr,
  es,
};

// Export default translations (English)
export default en;
