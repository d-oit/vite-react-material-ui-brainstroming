import de from './de';
import en from './en';
import es from './es';
import fr from './fr';

// Export all translations
export const translations = {
  en,
  de,
  fr,
  es,
};

// Export default translations (English)
export default en;
